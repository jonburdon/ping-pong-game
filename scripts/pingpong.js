pingPongGame = function(pingpong) {
    if (pingpong === "Pong") {
        return "Ping";
    }
    else if (pingpong === "Ping"){
        return "Pong";
    }
    else {
        return "error";
    }
};